# BananaWiki

A lightweight, self-hosted wiki application built with Flask and SQLite. BananaWiki provides a simple yet powerful platform for creating and managing wiki content with Markdown support, role-based access control, and an invite-only signup system.

## Features

- **Markdown Editing** — Write content using Markdown with support for tables, fenced code blocks, table of contents, and newline-to-break conversion
- **Category Organization** — Organize pages into hierarchical categories with customizable sort order
- **Page History** — Track revisions with optional page history viewer and revert capability
- **Draft Autosave** — Automatic draft saving with conflict detection when multiple users edit the same page
- **Image Uploads** — Upload images via drag-and-drop or file picker with automatic Markdown insertion
- **Role-Based Access Control** — Three user roles (user, editor, admin) with granular permissions
- **Invite Code System** — Control signups with time-limited invite codes generated by admins
- **Customizable Appearance** — Configure site name and color scheme through the admin panel
- **Mobile Responsive** — Collapsible sidebar with drag-to-resize support

## Tech Stack

- **Backend:** Python / [Flask 3.1](https://flask.palletsprojects.com/)
- **Database:** SQLite (WAL mode)
- **Markdown:** [Python-Markdown](https://python-markdown.github.io/) with [Bleach](https://bleach.readthedocs.io/) sanitization
- **Image Processing:** [Pillow](https://pillow.readthedocs.io/)
- **Frontend:** Vanilla HTML, CSS, and JavaScript

## Prerequisites

- Python 3.8 or higher
- pip (Python package manager)

## Installation

1. **Clone the repository:**

   ```bash
   git clone https://github.com/OverloadedTech/BananaWiki.git
   cd BananaWiki
   ```

2. **Install dependencies:**

   ```bash
   pip install -r requirements.txt
   ```

3. **Run the application:**

   ```bash
   python app.py
   ```

4. **Open your browser** and navigate to `http://localhost:8080`. You will be directed to the setup page to create the first admin account.

## Initial Setup

On the first run, BananaWiki presents a setup wizard:

1. Choose a **username** (3–50 characters)
2. Set a **password** (minimum 6 characters)
3. Confirm the password

This creates the initial admin account and marks setup as complete. Subsequent users are added via invite codes or the admin panel.

## Configuration

Edit `config.py` to customize the application. Key options include:

| Setting | Default | Description |
|---|---|---|
| `PORT` | `8080` | Server port |
| `USE_PUBLIC_IP` | `True` | Bind to `0.0.0.0` for network access |
| `USE_LOCAL_IP` | `True` | Also listen on `127.0.0.1` |
| `CUSTOM_DOMAIN` | `None` | Optional custom domain for display |
| `DATABASE_PATH` | `instance/bananawiki.db` | SQLite database file path |
| `MAX_UPLOAD_SIZE` | `16 * 1024 * 1024` | Maximum upload file size (16 MB) |
| `ALLOWED_EXTENSIONS` | `png, jpg, jpeg, gif, webp` | Permitted upload file types |
| `LOGGING_ENABLED` | `True` | Enable file-based logging |
| `LOG_FILE` | `logs/bananawiki.log` | Log file location |
| `PAGE_HISTORY_ENABLED` | `False` | Enable the page history viewer |
| `INVITE_CODE_EXPIRY_HOURS` | `48` | Hours before invite codes expire |

## User Roles

BananaWiki uses three roles with increasing levels of access:

| Role | Permissions |
|---|---|
| **User** | View pages |
| **Editor** | View pages, create pages, edit pages, manage categories, revert page history |
| **Admin** | All editor permissions plus: manage users, generate invite codes, configure site settings, delete pages |

New users are assigned the **user** role by default when signing up with an invite code. Admins can change roles from the admin panel.

## Security

- **HTML Sanitization** — All Markdown output is sanitized with Bleach using a strict allowlist of HTML tags and attributes
- **CSRF Protection** — All forms and AJAX requests are protected with Flask-WTF CSRF tokens
- **Password Hashing** — Passwords are hashed using Werkzeug's security functions
- **Secure File Uploads** — Filenames are sanitized with `secure_filename()`, SVG uploads are blocked to prevent XSS, and file type validation is enforced
- **Input Validation** — Length limits are enforced on usernames (3–50 chars), page titles (max 200), category names (max 100), and passwords (min 6)
- **Invite-Only Signups** — New user registration requires a valid, unexpired invite code

## API Endpoints

BananaWiki exposes the following internal API routes (all require authentication):

| Endpoint | Method | Description |
|---|---|---|
| `/api/preview` | POST | Preview Markdown as rendered HTML |
| `/api/draft/save` | POST | Save a draft for a page |
| `/api/draft/load/<page_id>` | GET | Load a saved draft |
| `/api/draft/others/<page_id>` | GET | Check for other users' drafts on a page |
| `/api/draft/transfer` | POST | Transfer another user's draft to your account |
| `/api/draft/delete` | POST | Delete a draft |
| `/api/upload` | POST | Upload an image |
| `/api/upload/delete` | POST | Delete an uploaded image |

## Development

### Running Tests

Tests use [pytest](https://docs.pytest.org/) with an isolated temporary database:

```bash
pip install pytest
python -m pytest tests/ -v
```

### Project Structure

```
BananaWiki/
├── app.py              # Flask application and routes
├── db.py               # Database schema and queries
├── config.py           # Application configuration
├── wiki_logger.py      # Logging utility
├── requirements.txt    # Python dependencies
├── app/
│   ├── static/
│   │   ├── css/        # Stylesheets
│   │   ├── js/         # Client-side JavaScript
│   │   └── uploads/    # User-uploaded images
│   └── templates/
│       ├── base.html   # Base layout with sidebar
│       ├── auth/       # Login, signup, setup templates
│       ├── wiki/       # Page view, edit, create, history templates
│       ├── account/    # Account settings template
│       └── admin/      # Admin panel templates
├── instance/           # Database and secret key (gitignored)
├── logs/               # Application logs (gitignored)
└── tests/              # Test suite
```