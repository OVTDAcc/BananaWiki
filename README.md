# BananaWiki

A lightweight, self-hosted wiki application built with Flask and SQLite. BananaWiki provides a clean, fast platform for creating and managing wiki content with Markdown support, role-based access control, an invite-only signup system, and an admin announcement banner system.

## Screenshots

<table>
<tr>
<td><strong>Login page</strong><br><img src="https://github.com/user-attachments/assets/d9fad2c1-ced2-4320-9c1b-f03603df5d11" alt="Login page"></td>
<td><strong>Home — wiki view</strong><br><img src="https://github.com/user-attachments/assets/ab13800e-08c4-425e-809c-05b1d1ef3599" alt="Home page"></td>
</tr>
<tr>
<td><strong>Split-pane Markdown editor with live preview</strong><br><img src="https://github.com/user-attachments/assets/e02c2bad-b065-4131-a869-1c2b15cb3219" alt="Markdown editor"></td>
<td><strong>Page revision history</strong><br><img src="https://github.com/user-attachments/assets/b60f7a48-eb1a-4bd2-8cb7-8d220d364559" alt="Page history"></td>
</tr>
<tr>
<td><strong>Admin — manage users</strong><br><img src="https://github.com/user-attachments/assets/c2385adb-c6bb-46ee-86d0-ceb55f16a2c4" alt="Admin users"></td>
<td><strong>Admin — site settings &amp; theme colors</strong><br><img src="https://github.com/user-attachments/assets/9f2cdf61-c944-4f3b-91f0-b2cc006bead5" alt="Admin settings"></td>
</tr>
<tr>
<td><strong>Admin — announcement manager</strong><br><img src="https://github.com/user-attachments/assets/3438ed27-4b4e-4f2d-b76c-ece9e537419a" alt="Admin announcements"></td>
<td><strong>Account settings &amp; admin shortcuts</strong><br><img src="https://github.com/user-attachments/assets/05afee31-38cb-47b3-8c01-45f89052207e" alt="Account settings"></td>
</tr>
<tr>
<td colspan="2"><strong>Announcement banner — orange (1/2) and red (2/2) variants with navigation</strong><br><img src="https://github.com/user-attachments/assets/1be5bb61-7ae5-48aa-9bbd-47c513b581d0" alt="Announcement banner"></td>
</tr>
</table>

## Features

- **Markdown Editing** — Write content using Markdown with support for tables, fenced code blocks, table of contents, and newline-to-break conversion
- **Split-Pane Editor** — Live rendered preview alongside the editor with a full formatting toolbar and image drop zone
- **Category Organization** — Organize pages into hierarchical categories with collapsible sidebar navigation
- **Page History** — Full revision history with per-edit summaries, diff viewing, and one-click revert
- **Draft Autosave** — Automatic draft saving every few seconds; conflict detection when multiple users edit the same page
- **Image Uploads** — Upload images via drag-and-drop or file picker with automatic Markdown insertion
- **Role-Based Access Control** — Three user roles (user, editor, admin) with granular permissions
- **Invite Code System** — Control signups with time-limited invite codes generated by admins
- **Announcement Banners** — Admins can post site-wide announcements with color themes (red/orange/yellow/blue/green), text sizes, visibility controls (everyone/logged-in/logged-out), optional expiry dates, and Markdown formatting. Users can navigate multiple announcements and dismiss them for the session
- **Customizable Appearance** — Configure site name and full color scheme (primary, secondary, accent, text, sidebar, background) through the admin panel
- **Telegram Sync** — Automatically back up runtime data (database, uploads, config, logs) to Telegram on significant changes
- **Mobile Responsive** — Collapsible sidebar with drag-to-resize support on desktop

## Tech Stack

- **Backend:** Python / [Flask 3.1](https://flask.palletsprojects.com/) / [Gunicorn](https://gunicorn.org/)
- **Database:** SQLite (WAL mode)
- **Markdown:** [Python-Markdown](https://python-markdown.github.io/) with [Bleach](https://bleach.readthedocs.io/) sanitization
- **Image Processing:** [Pillow](https://pillow.readthedocs.io/)
- **Frontend:** Vanilla HTML, CSS, and JavaScript

## Prerequisites

- Python 3.9 or higher
- pip (Python package manager)

## Quick Start

```bash
git clone https://github.com/OverloadedTech/BananaWiki.git
cd BananaWiki
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
gunicorn wsgi:app -c gunicorn.conf.py
```

Open your browser and navigate to `http://<your-server-ip>:5001`. You will be directed to the setup page to create the first admin account.

## Initial Setup

On the first run, BananaWiki presents a setup wizard:

1. Choose a **username** (3–50 characters)
2. Set a **password** (minimum 6 characters)
3. Confirm the password

This creates the initial admin account and marks setup as complete. Subsequent users are added via invite codes or the admin panel.

## Configuration

Edit `config.py` to customize the application. Key options include:

| Setting | Default | Description |
|---|---|---|
| `PORT` | `5001` | Server port |
| `USE_PUBLIC_IP` | `True` | Bind to `0.0.0.0` for network access |
| `CUSTOM_DOMAIN` | `None` | Domain or subdomain (e.g., `wiki.example.com`) |
| `PROXY_MODE` | `False` | Enable when behind Cloudflare or a reverse proxy |
| `SSL_CERT` | `None` | Path to SSL certificate file (optional) |
| `SSL_KEY` | `None` | Path to SSL private key file (optional) |
| `DATABASE_PATH` | `instance/bananawiki.db` | SQLite database file path |
| `MAX_UPLOAD_SIZE` | `16 * 1024 * 1024` | Maximum upload file size (16 MB) |
| `ALLOWED_EXTENSIONS` | `png, jpg, jpeg, gif, webp` | Permitted upload file types |
| `LOGGING_ENABLED` | `True` | Enable file-based logging |
| `LOG_FILE` | `logs/bananawiki.log` | Log file location |
| `PAGE_HISTORY_ENABLED` | `True` | Enable the page history viewer |
| `INVITE_CODE_EXPIRY_HOURS` | `48` | Hours before invite codes expire |
| `SYNC` | `False` | Enable Telegram backup sync (strongly recommended) |
| `SYNC_TOKEN` | `""` | Telegram Bot API token from @BotFather |
| `SYNC_USERID` | `""` | Telegram user/chat ID to receive backups |

## User Roles

BananaWiki uses three roles with increasing levels of access:

| Role | Permissions |
|---|---|
| **User** | View pages |
| **Editor** | View pages, create pages, edit pages, manage categories, revert page history |
| **Admin** | All editor permissions plus: manage users, generate invite codes, configure site settings, post announcements, delete pages |

New users are assigned the **user** role by default when signing up with an invite code. Admins can change roles from the admin panel.

## Admin Features

All admin functions are accessible from **Account → Admin Operations**.

### User Management (`/admin/users`)

- View all users with role, status, and last login time
- Create users directly (bypassing invite codes)
- Change user roles or suspend accounts
- View per-user audit log (`📋` button)
- Filter users by role or status

### Invite Codes (`/admin/codes`)

- Generate time-limited invite codes (48 hours by default)
- View active and expired/used codes
- Delete codes before they are used

### Site Settings (`/admin/settings`)

- Rename the wiki
- Customize the full color palette: primary, secondary, accent, text, sidebar, and background colors via color picker or hex input

### Announcements (`/admin/announcements`)

- Create announcements with full **Markdown support** (bold, links, lists, etc.)
- Choose a **banner color**: red, orange, yellow, blue, or green
- Set the **text size**: small, normal, or large
- Control **visibility**: show to everyone, logged-in users only, or logged-out users only
- Set an optional **expiry date/time** — the banner disappears automatically when it expires, or leave it indefinite
- Toggle announcements **active/inactive** without deleting them
- Users navigate multiple banners with **‹ / ›** arrow buttons and a live counter
- Users can **✕ dismiss** any banner for the rest of their browser session

Long announcements (over 500 characters of Markdown source) are automatically truncated in the banner with a **Read more →** link to a dedicated full-content page.

## Production Deployment

### Running with systemd (recommended)

A systemd service keeps BananaWiki running in a virtual environment and automatically restarts it after crashes or server reboots.

**1. Install BananaWiki and create a venv:**
```bash
sudo mkdir -p /opt/BananaWiki
sudo chown www-data:www-data /opt/BananaWiki
cd /opt/BananaWiki

# Clone or copy your files here
git clone https://github.com/OverloadedTech/BananaWiki.git .

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

**2. Edit `config.py`** to set your port and domain:
```python
PORT = 5001                              # pick any free port
USE_PUBLIC_IP = False                    # False when using nginx on the same machine
CUSTOM_DOMAIN = "wiki.example.com"       # or None for IP-only access
PROXY_MODE = True                        # if using Cloudflare or a reverse proxy
```

**3. Install the systemd service:**
```bash
sudo cp bananawiki.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable bananawiki     # start on boot
sudo systemctl start bananawiki      # start now
```

**Manage the service:**
```bash
sudo systemctl status bananawiki     # check status
sudo systemctl restart bananawiki    # restart (e.g. after config changes)
sudo systemctl stop bananawiki       # stop
journalctl -u bananawiki -f          # view live logs
```

> **Tip:** Edit the `User`, `Group`, and `WorkingDirectory` fields in `bananawiki.service` if your installation path or user differs from the defaults.

### Running manually with Gunicorn

```bash
source venv/bin/activate
gunicorn wsgi:app -c gunicorn.conf.py
```

Or with a custom bind address:
```bash
gunicorn wsgi:app --bind 0.0.0.0:5001 --workers 2
```

The `gunicorn.conf.py` file reads `HOST`, `PORT`, and `PROXY_MODE` from `config.py` automatically.

### Development server

For local development only, you can run the Flask dev server directly:
```bash
python app.py
```

> **Warning:** The Flask development server is single-threaded and not suitable for production. Always use Gunicorn (via systemd or manually) for production deployments.

### Multi-App Hosting

Run multiple apps on the same server by assigning each a different port:

| App | Port | Domain |
|---|---|---|
| BananaWiki | 5001 | `wiki.example.com` |
| Another app | 5002 | `app.example.com` |
| Yet another | 5003 | `tools.example.com` |

Each app's `config.py`:
```python
PORT = 5001   # unique port per app
```

Access directly via `http://<server-ip>:5001`, or set up Cloudflare/reverse proxy to route domains.

### Custom Domains via Cloudflare

[Cloudflare](https://www.cloudflare.com/) provides free DNS, CDN, and SSL. This is the simplest way to put a domain in front of your app.

**1. Add your domain to Cloudflare** and update your registrar's nameservers.

**2. Create a DNS A record** pointing to your server's public IP:

| Type | Name | Content | Proxy |
|---|---|---|---|
| `A` | `wiki` | `YOUR_SERVER_IP` | Proxied (orange cloud) |

**3. Set Cloudflare SSL/TLS mode** to **Flexible** (Cloudflare handles HTTPS for visitors and connects to your server via HTTP).

> **Security note:** Flexible mode means traffic between Cloudflare and your server is unencrypted. For sensitive content, use **Full** or **Full (Strict)** mode with an SSL certificate on your server (see [Direct HTTPS](#direct-https-without-cloudflare)).

**4. Configure BananaWiki** (`config.py`):
```python
PORT = 5001
USE_PUBLIC_IP = True              # True when Cloudflare connects directly (no nginx)
PROXY_MODE = True
CUSTOM_DOMAIN = "wiki.example.com"
```

That's it — Cloudflare handles HTTPS, certificates, and CDN. Visitors access `https://wiki.example.com` and Cloudflare routes to your server on `http://<ip>:5001`.

**Recommended Cloudflare settings:**
- **Always Use HTTPS**: On
- **Minimum TLS Version**: TLS 1.2
- **Automatic HTTPS Rewrites**: On

### IP-Only Access (No Domain)

No domain configuration is needed:
```python
PORT = 5001
USE_PUBLIC_IP = True       # default
CUSTOM_DOMAIN = None       # default
```

Access at `http://<your-server-ip>:5001`.

> **Note:** `USE_PUBLIC_IP = True` binds to all network interfaces (`0.0.0.0`), making the server reachable from other machines. Ensure appropriate firewall rules are in place. Set `USE_PUBLIC_IP = False` to restrict access to `127.0.0.1` (localhost only).

### Direct HTTPS (without Cloudflare)

If you want to terminate TLS directly (e.g., with Let's Encrypt), set the SSL paths in `config.py`:
```python
PORT = 443
SSL_CERT = "/etc/letsencrypt/live/wiki.example.com/fullchain.pem"
SSL_KEY  = "/etc/letsencrypt/live/wiki.example.com/privkey.pem"
```

Or use a reverse proxy (nginx/Caddy) for TLS termination:

**Caddy** (automatic HTTPS):
```
wiki.example.com {
    reverse_proxy 127.0.0.1:5001
}
```

**Nginx:**
```nginx
server {
    listen 443 ssl;
    server_name wiki.example.com;
    ssl_certificate     /etc/letsencrypt/live/wiki.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/wiki.example.com/privkey.pem;
    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

When using a reverse proxy on the same machine, set `USE_PUBLIC_IP = False` so Gunicorn only listens on localhost and the port is not exposed directly:
```python
PORT = 5001
USE_PUBLIC_IP = False             # bind to 127.0.0.1 only (nginx forwards traffic)
PROXY_MODE = True
CUSTOM_DOMAIN = "wiki.example.com"
```

## Security

- **HTML Sanitization** — All Markdown output is sanitized with Bleach using a strict allowlist of HTML tags and attributes
- **CSRF Protection** — All forms and AJAX requests are protected with Flask-WTF CSRF tokens
- **Password Hashing** — Passwords are hashed using Werkzeug's security functions (bcrypt via `generate_password_hash`)
- **Login Rate Limiting** — Failed login attempts are rate-limited per IP (5 attempts per 60 seconds) using a persistent SQLite store shared across Gunicorn workers
- **Session Fixation Prevention** — `session.clear()` is called before setting the user session on login
- **Secure File Uploads** — Filenames are sanitized with `secure_filename()`, SVG uploads are blocked to prevent XSS, and file type validation is enforced
- **Input Validation** — Length limits enforced on usernames (3–50 chars), page titles (max 200), category names (max 100), passwords (min 6), and announcement content (max 2000)
- **Invite-Only Signups** — New user registration requires a valid, unexpired invite code
- **Security Headers** — `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, and `Content-Security-Policy` headers set on every response

## API Endpoints

BananaWiki exposes the following internal API routes (all require authentication):

| Endpoint | Method | Description |
|---|---|---|
| `/api/preview` | POST | Preview Markdown as rendered HTML |
| `/api/draft/save` | POST | Save a draft for a page |
| `/api/draft/load/<page_id>` | GET | Load a saved draft |
| `/api/draft/others/<page_id>` | GET | Check for other users' drafts on a page |
| `/api/draft/transfer` | POST | Transfer another user's draft to your account |
| `/api/draft/delete` | POST | Delete a draft |
| `/api/upload` | POST | Upload an image |
| `/api/upload/delete` | POST | Delete an uploaded image |

## Development

### Running Tests

Tests use [pytest](https://docs.pytest.org/) with an isolated temporary database:

```bash
pip install pytest
python -m pytest tests/ -v
```

### Project Structure

```
BananaWiki/
├── app.py              # Flask application and routes
├── wsgi.py             # WSGI entry point for Gunicorn
├── gunicorn.conf.py    # Gunicorn configuration
├── bananawiki.service  # systemd service file
├── db.py               # Database schema and queries
├── config.py           # Application configuration
├── sync.py             # Telegram sync/backup module
├── wiki_logger.py      # Logging utility
├── requirements.txt    # Python dependencies
├── app/
│   ├── static/
│   │   ├── css/        # Stylesheets
│   │   ├── js/         # Client-side JavaScript
│   │   └── uploads/    # User-uploaded images
│   └── templates/
│       ├── base.html                  # Base layout with sidebar
│       ├── _announcements_bar.html    # Announcement banner partial
│       ├── auth/       # Login, signup, setup templates
│       ├── wiki/       # Page view, edit, create, history, announcement templates
│       ├── account/    # Account settings template
│       └── admin/      # Admin panel templates (users, settings, codes, announcements)
├── instance/           # Database and secret key (gitignored)
├── logs/               # Application logs (gitignored)
└── tests/              # Test suite
```